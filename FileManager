function createGoogleForm() {
  // 建立表單
}

function addBasicQuestions(form) {
  // 基本資料
}

function addSportQuestions(form) {
  // 球類選擇
}

function addPartnerSections(form) {
  // 籃球/羽球/桌球分流
}

function saveFormInfo(form) {
  // 儲存 Form ID、網址
}

form.addListItem()
    .setTitle("所屬會所")
    .setChoiceValues(CONFIG.CHURCHES);
